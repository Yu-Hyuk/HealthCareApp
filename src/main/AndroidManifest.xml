<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_heart"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_heart_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project2"
        android:usesCleartextTraffic="true">
        <activity android:name=".datafirebase1"></activity>
        <activity android:name=".Exercise4" />
        <activity android:name=".Exercise3" />
        <activity android:name=".exercise2" />
        <activity android:name=".Exercise1"></activity>
        <activity android:name=".FirebaseExample" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".hospital" />
        <activity android:name=".bmi_infoActivity" />
        <activity android:name=".infoActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".homeActivity" />
        <activity android:name=".bmiActivity" />
    </application>

</manifest>